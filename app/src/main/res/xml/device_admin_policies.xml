<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Intentionally empty: just being a device admin at all is what
             makes uninstall require deactivation first. No extra policies
             (password rules, camera, etc.) are needed or used. -->
    </uses-policies>
</device-admin>
